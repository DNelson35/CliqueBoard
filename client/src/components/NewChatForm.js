import React from 'react'

function NewChatForm() {
    // the hope here is to separate some of the chat functionality for handling a new recipient request and seting the chat as to tak the place of this menue with the new message sent to the recipient.

    // another problem cam up if i send a message and the recipient is not already somwhere in the messanger they will not recieve the message.
  return (
    <div>NewChatForm</div>
  )
}

export default NewChatForm